# Token-efficient long-horizon agentic executor with the best tool-routing on the
# roster. Use it to drive MCP tools, A2A calls, browser and shell workflows.
model:
  chat:
    provider: ollama_cloud
    name: kimi-k2.7-code
    api_base: https://ollama.com
    ctx_length: 256000
    limit_requests: 8
  utility:
    provider: ollama_cloud
    name: gpt-oss:20b-cloud
    api_base: https://ollama.com
