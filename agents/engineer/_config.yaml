# The dev swarm - run ~4 in parallel. Kimi-K2.7-Code: frontier coder with ~30%
# fewer thinking tokens than K2.6 -> the most verified-stories-per-slot-hour on
# the roster, plus the best tool-routing (MCP-Mark Verified 81.1%). With no
# per-token pricing, quality-per-slot beats cheap-per-token: no gpt-oss here.
model:
  chat:
    provider: ollama_cloud
    name: kimi-k2.7-code
    api_base: https://ollama.com
    ctx_length: 256000
    limit_requests: 20
  utility:
    provider: ollama_cloud
    name: nemotron-3-super
    api_base: https://ollama.com
